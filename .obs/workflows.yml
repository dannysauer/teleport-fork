rebuild_gravitational_teleport:
  steps:
    - trigger_services:
        project: home:dannysauer
        package: gravitational_teleport
  filters:
    event: push
    branches:
      only:
        - autobuild
